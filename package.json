{
  "name": "council-moderator-render",
  "version": "2.0.0",
  "private": true,
  "description": "Keyless access to a Vertex AI Agent Engine (council_moderator): Render-hosted React SPA + relay, forwarding to a Cloud Run proxy that authenticates via the GCP metadata server.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build",
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "test": "node --test e2e/offline.test.mjs",
    "test:offline": "node --test e2e/offline.test.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "google-auth-library": "^10",
    "jose": "^5.9.6"
  }
}
